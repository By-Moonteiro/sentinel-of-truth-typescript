import path from 'path';

// Caminho para o DB
export const DATA = path.join(__dirname, '..', '..', 'data', 'news.db');

// Caminho para o relatório
export const REPORT = path.join(__dirname, '..', '..', 'data', 'report.txt');
